apiVersion: apps/v1
kind: Deployment
metadata:
  name: netops-ops-console
  namespace: netops-console
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netops-ops-console
  template:
    metadata:
      labels:
        app: netops-ops-console
    spec:
      nodeSelector:
        kubernetes.io/hostname: r450
      containers:
        - name: netops-ops-console
          image: netops-ops-console:0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: NETOPS_RUNTIME_ROOT
              value: /data/netops-runtime
          readinessProbe:
            httpGet:
              path: /api/healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - name: netops-runtime
              mountPath: /data/netops-runtime
      volumes:
        - name: netops-runtime
          hostPath:
            path: /data/netops-runtime
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: netops-ops-console
  namespace: netops-console
spec:
  selector:
    app: netops-ops-console
  ports:
    - name: http
      port: 80
      targetPort: http
